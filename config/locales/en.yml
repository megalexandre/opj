en:
  activerecord:
    errors:
      models:
        address:
          attributes:
            base:
              restrict_dependent_destroy:
                has_one: "Cannot delete this address because it is linked to an existing customer."
